// 104 - Max Depth of Binary Tree

import { bfs } from "@templates/tree_traversal_template";
import { TreeNode } from "@structures/TreeNode";

function maxDepth(root: TreeNode | null): number {

  return -1;
}